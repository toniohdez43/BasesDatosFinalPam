@model ProyectoFinalPam.Models.ServicioBecado

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>ServicioBecado</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.IDOrg)

        @Html.HiddenFor(model => model.IDBecado)

        @Html.HiddenFor(model => model.Dias)

        @Html.HiddenFor(model => model.HorarioInicio)

        @Html.HiddenFor(model => model.HorarioFin)

        @Html.HiddenFor(model => model.Actividades)

        @Html.HiddenFor(model => model.FechaInicio)

        @Html.HiddenFor(model => model.FechaFin)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
